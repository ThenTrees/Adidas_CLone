// let options = document.querySelectorAll(".options");
let options = document.querySelectorAll(".option");
let icon = options.getClassByTagName("i");

for (let o of options) {
    o.addEventListener("click", () => {
    });
}
